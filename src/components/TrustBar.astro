---
---

<section class="trust-bar" aria-labelledby="trust-heading">
  <div class="container trust-bar-inner">
    <p class="trust-bar-eyebrow">Affordable fire safety solutions</p>
    <h2 id="trust-heading" class="trust-bar-heading">Meet Our Clients</h2>
  </div>
</section>

<style>
  .trust-bar {
    background-color: var(--color-charcoal);
    color: var(--color-white);
    padding-block: 5em;
  }

  .trust-bar-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1.5em;
  }

  .trust-bar-eyebrow {
    font-family: var(--font-accent);
    font-size: var(--fs-sm); /* 16px */
    color: var(--color-warm-white);
    opacity: 0.7;
    line-height: 1.2;
  }

  .trust-bar-heading {
    font-family: var(--font-accent);
    font-weight: 600;
    font-size: 2.75em; /* 44px */
    line-height: 1.2;
    color: var(--color-white);
  }

  @media (max-width: 767px) {
    .trust-bar-heading {
      font-size: 2em;
    }
  }
</style>
